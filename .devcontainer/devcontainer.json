{
	"name": "jasmin-telnet-dev",
	"dockerComposeFile": [
		"docker-compose.yml"
	],
	"service": "telnet",
	"workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
	"postCreateCommand": {
		"install": "pip install ."
	}
}
